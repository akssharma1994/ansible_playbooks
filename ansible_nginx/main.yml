---
  - name: "Installing and configure Nginx server"
    hosts: all
    become: yes
    gather_facts: true
  - name: "updating repos"
    yum: 
      name: "*"
      state: latest
  - name: "installing nginx "
    yum: 
      name: Nginx
      state: present